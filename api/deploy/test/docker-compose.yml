version: '3.0'
services:

  database:
    image: amsterdam/postgres
    environment:
      POSTGRES_PASSWORD: insecure
      POSTGRES_USER: external-data-scraper
      POSTGRES_DB: external-data-scraper

  tests:
    build: ../../
    links:
      - database
    environment:
      DATABASE_NAME: external-data-scraper
      DATABASE_USER: external-data-scraper
      DATABASE_PASSWORD: insecure
      ENVIRONMENT: test
    volumes:
        - ./run_tests.sh:/app/run_tests.sh
    command: /app/run_tests.sh
